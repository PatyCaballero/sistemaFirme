<%= form_with(model: puesto_registro, local: true) do |form| %>
  <% if puesto_registro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(puesto_registro.errors.count, "error") %> prohibited this puesto_registro from being saved:</h2>

      <ul>
      <% puesto_registro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div id="main-container" class="container">
      <div class="row">
      <div class="col-xs-12">
          <div class="col-xs-3">
          </div>
          <div class="jumbotron">
            <div class="container">
          <div class="col-xs-4">
                <form class="form-inline">

                  <div class="field">
                    <%= form.text_field :puestoNombre, id: :puesto_registro_puestoNombre, class: "form-control", placeholder: "Puesto" %><br><br> 

                 </div>

                  <div class="field">
                    <%= form.text_field :direccion, id: :puesto_registro_direccion, class: "form-control", placeholder: "Direccion" %><br><br> 
                  </div>

              <div class="field">
                    <%= form.text_field :turno, id: :puesto_registro_turno, class:"form-control", placeholder:"Turno" %><br><br> 
                  </div>
                        </div>
          <div class="col-xs-3">
          </div>
          <div class="col-xs-4">

                  <div class="field">
                    <%= form.time_select :horaEntrada, id: :puesto_registro_horaEntrada, class:"form-control", placeholder:"Hora Entrada" %><br><br><br><br> 
                  </div>

                  <div class="field">
                    <%= form.time_select :horaSalida, id: :puesto_registro_horaSalida, class:"form-control", placeholder:"Hora Salida" %>
                  </div>
          </div>
          <div class="col-xs-2">
          </div>
          <div class="col-xs-3">
          </div>
          </div>
          </div>
      <div class="actions">
          <%= form.submit %>
      </div>
      <% end %>
