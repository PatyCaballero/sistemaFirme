 <%= form_with(model: presupuestooo, local: true) do |form| %>
  <% if presupuestooo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(presupuestooo.errors.count, "error") %> prohibited this presupuestooo from being saved:</h2>

      <ul>
      <% presupuestooo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div id="main-container" class="container">
      <div class="row">
      <div class="col-xs-12">
          <div class="col-xs-3">
          </div>
          <div class="jumbotron">
            <div class="container">
          <div class="col-xs-4">
              <form class="form-inline">

                  <div class="field">
                    <%= form.label :Fecha %>
                    <%= form.date_field :fecha, id: :presupuestooo_fecha, class: "form-control", placeholder:"Fecha" %>
                  </div><br><br>

                  <div class="field">
                    <%= form.label :Turno %>
                    <%= form.collection_select(:turno_id, Turno.all, :id, :nombre) %>
                  </div><br><br>

                  <div class="field">
                    <%= form.label :CantidadHora %>
                    <%= form.number_field :cantHora, id: :presupuestooo_cantHora %>
                  </div><br><br>
         
                  <div class="field">
                    <%= form.label :CantidadGuardia %>
                    <%= form.number_field :cantGuardia, id: :presupuestooo_cantGuardia %>
                  </div><br><br>

                 <!-- <div class="field">
                    <%= form.label :MontoMensual %>
                    <%= form.text_field :montoMensual, id: :presupuestooo_montoMensual %>
                  </div><br><br> -->
              

          </div>
          <div class="col-xs-3">
          </div>
      </div>
    </div>

                <div class="col-xs-3">
                  <div class="actions">
                    <%= form.submit "Guardar", :class=>"button-guardar" %>
                  </div>
                  <% end %>
                </div>



