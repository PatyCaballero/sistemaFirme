<%= form_with(model: producto, local: true) do |form| %>
  <% if producto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(producto.errors.count, "error") %> prohibited this producto from being saved:</h2>

      <ul>
      <% producto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div id="main-container" class="container">
      <div class="row">
      <div class="col-xs-12">
          <div class="col-xs-3">
          </div>
          <div class="jumbotron">
            <div class="container">
          <div class="col-xs-4">
              <form class="form-inline">

              <div class="field">
                    <%= form.label "Categoria" %>
                    <%= form.collection_select(:categoria_id, Categoria.all, :id, :nombre) %>
              </div><br><br>

              <div class="field">
                  <%= form.label :Identificador %>
                  <%= form.text_field :identificador, id: :producto_identificador %>
              </div><br><br>

              <div class="field">
                  <%= form.label :Nombre %>
                  <%= form.text_field :nomProducto, id: :producto_nomProducto %>
                </div><br><br>

          </div>
          <div class="col-xs-3">
          </div>
          <div class="col-xs-4">

                 <div class="field">
                  <%= form.label :Cantidad_Total%>
                  <%= form.number_field :cantidad, id: :producto_cantidad %>
                </div> <br><br>
                
                <div class="field">
                  <%= form.label :caracteristica %>
                  <%= form.text_area :caracteristica, id: :producto_caracteristica %>
                </div><br><br>
          </div>
          <div class="col-xs-3">
          </div>
      </div>
    </div>

        <div class="col-xs-3">
                <div class="actions">
                  <%= form.submit "Guardar", :class=>"button-guardar" %>
                </div>
              <% end %>
        </div>
