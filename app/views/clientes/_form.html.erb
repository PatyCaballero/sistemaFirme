<%= form_with(model: cliente, local: true) do |form| %>
  <% if cliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cliente.errors.count, "error") %> prohibited this cliente from being saved:</h2>

      <ul>
      <% cliente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div id="main-container" class="container">
      <div class="row">
      <div class="col-xs-12">
          <div class="col-xs-3">
          </div>
          <div class="jumbotron">
            <div class="container">
          <div class="col-xs-4">
                <form class="form-inline">

                        <div class="field">
                          <%= form.text_field :nombre, id: :cliente_nombre, class:"form-control", placeholder:"Cliente" %><br><br>
                        </div>

                        <div class="field">
                          <%= form.text_field :num_ruc, id: :cliente_num_ruc, class:"form-control", placeholder:"Nº Ruc" %><br><br>
                        </div>

          </div>
          <div class="col-xs-3">
          </div>
          <div class="col-xs-4">

                        <div class="field">
                          <%= form.text_field :telefono, id: :cliente_telefono, class:"form-control", placeholder:"Telefono" %><br><br>
                        </div>
                        <div class="field">
                          <%= form.text_field :direccion, id: :cliente_direccion, class:"form-control", placeholder:"Direccion" %><br><br>
                        </div>

         </div>
         <div class="col-xs-5">
         </div>
          <div class="col-xs-3">
              <div class="field">
                          <%= form.label "Estado" %>
                          <%= form.check_box :estado, id: :cliente_estado %>
                        </div>
          </div>
          <div class="col-xs-2">
          </div>
          <div class="col-xs-3">
          </div>
      </div>
    </div>

        <div class="col-xs-3">
                        <div class="actions">
                          <%= form.submit %>
                        </div>
                        <% end %>
        </div>
