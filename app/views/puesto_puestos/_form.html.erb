<%= form_with(model: puesto_puesto, local: true) do |form| %>
  <% if puesto_puesto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(puesto_puesto.errors.count, "error") %> prohibited this puesto_puesto from being saved:</h2>

      <ul>
      <% puesto_puesto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
