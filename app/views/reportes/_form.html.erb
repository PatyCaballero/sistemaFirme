<%= form_with(model: reporte, local: true) do |form| %>
  <% if reporte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reporte.errors.count, "error") %> prohibited this reporte from being saved:</h2>

      <ul>
      <% reporte.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :horaEntrada %>
    <%= form.time_select :horaEntrada, id: :reporte_horaEntrada %>
  </div>

  <div class="field">
    <%= form.label :horaSalida %>
    <%= form.time_select :horaSalida, id: :reporte_horaSalida %>
  </div>

  <div class="field">
    <%= form.label :estado %>
    <%= form.text_field :estado, id: :reporte_estado %>
  </div>

  <div class="field">
    <%= form.label :novedad %>
    <%= form.text_field :novedad, id: :reporte_novedad %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
